import type React from "react"
import type { Metadata } from "next"
import { Inter, Geist_Mono } from "next/font/google"
import { Analytics } from "@vercel/analytics/next"
import { DevToastProvider } from "@/components/dev-toast-provider"
import "./globals.css"

// Switched to Inter font for professional look
const _inter = Inter({ subsets: ["latin"] })
const _geistMono = Geist_Mono({ subsets: ["latin"] })

export const metadata: Metadata = {
  title: "Aslea - Digital Staffing Agency for Indian Business",
  description:
    "The first Digital Staffing Agency for Indian Business. Get 24/7 Receptionists, Sales Reps, and Assistants who speak your language.",
  generator: "v0.app",
  icons: {
    icon: [
      {
        url: "/icon-light-32x32.png",
        media: "(prefers-color-scheme: light)",
      },
      {
        url: "/icon-dark-32x32.png",
        media: "(prefers-color-scheme: dark)",
      },
      {
        url: "/icon.svg",
        type: "image/svg+xml",
      },
    ],
    apple: "/apple-icon.png",
  },
}

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode
}>) {
  return (
    <html lang="en">
      <body className={`font-sans antialiased`}>
        <DevToastProvider>{children}</DevToastProvider>
        <Analytics />
      </body>
    </html>
  )
}
